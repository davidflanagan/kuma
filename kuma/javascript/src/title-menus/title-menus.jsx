//@flow
import React from 'react';
import { css } from '@emotion/core';

type TitleMenuProps = {
    languages: Array<{ locale: string, title: string, slug: string }>
};

export default function TitleMenus(props: TitleMenuProps): React.Node {}
